%h1.uk-heading-small.uk-text-center 今日やる復習
.index 
  %hr/
  - if @reviews.empty?
    %h2.empty 今日の復習はありません
  - else
    = link_to "復習を開始する", user_reviewing_path, class: "uk-button uk-button-primary uk-width-1-1 uk-margin-small-bottom"
    %hr/
    - @reviews.each_with_index do |review, i|
      %ul
        %li
          = "復習：#{review.count}回目 学習日：#{review.lesson.created_at.strftime('%Y-%m-%d')}"
        %li 
          = "復習予定日:#{review.date}"
        %li
          = "内容: #{review.lesson.content}"
      %hr/